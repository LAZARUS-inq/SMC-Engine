#include "Application.h"

namespace SMC {

	Application::Application() {
		// Constructor implementation (if any)
	}
	Application::~Application() {
		// Destructor implementation (if any)
	}

	void Application::Run() {
		// Main application loop implementation (if any)
		while (true);
	}
}
